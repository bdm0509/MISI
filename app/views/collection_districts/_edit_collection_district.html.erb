<%= form_for(@collection_district, :html => { :class => "align validate_me" } ) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
	
  <fieldset class="align">
    <%= f.label :name, :class => "align required" %>: <%= f.text_field :name, :class => "align required", :size => 40 %><br />
  </fieldset>
  <fieldset class="align form_controls">
    <input type="reset" name="Reset Values" />
    <%= f.submit %>
  </fieldset>
<% end %>